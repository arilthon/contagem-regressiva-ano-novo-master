## Dom

## Selecionado elementos

```js
|Seleciona um elemento com document.querySelector(selectors)
|Retorna o primeiro elemento dentro do documento.

|Sintaxe:
const el = document.querySelector(".myclass");

|
```

## Objeto

### Date e Time

```js
| new Date() e objeto interno que armazena a  dat e hora e fornece métodos para
gerenciamento de data/hora

|Sintaxe:
const now = new Date() // Mostra data/hora atual.

const getYearTime = new Date('Janeiro 01,2023 00:00:00') //Criar objeto Date

console.log(now)

|
```

### Math

```js
|Math é um objeto embutido que tem propridades e métodos para constantes e funções
matemáticas.
|Todas propridade e métodos Math são estáticos.onde x e o argumento do método.


|Sintaxe:
const days = Math.floor(difference / 1000 / 60 / 60 / 24) //dia

```
